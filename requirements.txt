# Online Complaint Registration & Management System
# ---------------------------------------------------------------------------
# Every dependency below is free and open source. No package here requires a
# paid plan, a subscription, a trial, a credit card or a commercial licence.
# The licence of each package is stated so the zero-cost claim is auditable.
# ---------------------------------------------------------------------------

Django==5.2.17            # BSD-3-Clause  - web framework (MVT)
django-environ==0.14.0    # MIT           - reads settings from .env / environment
django-axes==8.3.1        # MIT           - brute-force login protection
whitenoise==6.12.0        # MIT           - static file serving without a CDN
reportlab==5.0.0          # BSD-3-Clause  - PDF report generation
PyMySQL==1.2.0            # MIT           - pure-python MySQL 8 driver (no compiler)
psycopg[binary]==3.3.4    # LGPL-3.0      - PostgreSQL driver (Vercel / Neon / Supabase)
gunicorn==26.0.0          # MIT           - WSGI server used inside the container

# --- Development / QA (safe to install everywhere, not needed at runtime) ---
coverage==7.15.4          # Apache-2.0    - test coverage measurement

# --- Transitive dependencies, pinned for reproducible builds ---
asgiref==3.12.1           # BSD-3-Clause
sqlparse==0.5.5           # BSD-3-Clause
tzdata==2026.3            # Public domain (IANA)
charset-normalizer==3.4.9 # MIT
packaging==26.3           # Apache-2.0 / BSD-2-Clause
